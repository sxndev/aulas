/* ESTRUTURA DO FOR
    
    for(inicializador; condição; incremento ){
        declaração
    }
    
    inicializador: vai ser usado para inicializar um contador de variáveis

    condição: vai ter uma expressão que vai ser avaliada a cada uma dessas interações do loop, se o resultado for true, a declaração será executada

    incremento:
    
    declaração: bloco de código que vai rodar quando o "for" ser executado
    */

    for(let i = 0; i < 10; i++){
        console.log("Olá Mundo!")
    }
    // enquanto o valor de i for menor que 10, uma nova mensagem será criada, e assim, adiciona mais um i

    for(let i =0; i < 10; i++){}