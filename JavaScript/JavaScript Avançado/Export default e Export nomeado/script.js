import { somar } from './operacoes-matematicas'

console.log(somar(1, 3))

// se usamos o export default não usamos as chaves ( {} )

// a exportação nomeada usa as chaves ( {} ) para especificar qual é o módulos JS que será exportado e importado, o nome do módulo na exportação deve ser o mesmo nome na importação